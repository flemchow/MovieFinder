//created by flemming
/**
 * interface that symbolizes the default Movie basis
 */
export interface Movie {
  title: string;
  release_date: number;
  overview: string;
  poster_path: string;
  genres: string[];
}
