//created by flemming
/**
 * this file is used to condense all of everything that needs to be exported into a single location.
 * cleans up the import statements in other files
 */

export { default as Login } from "./loginRoot";
export { default as LoginComponent } from "./login";
export { default as RegisterComponent } from "./register";
